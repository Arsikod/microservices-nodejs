export enum TicketsUrls {
  tickets = "/api/tickets",
  ticketId = "/api/tickets/:id",
}

export enum AuthUrls {
  currentUser = "/api/users/currentuser",
  signin = "/api/users/signin",
  signout = "/api/users/signout",
  signup = "/api/users/signup",
}
